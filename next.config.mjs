/** @type {import('next').NextConfig} */
const nextConfig = {
  // Placeholder-fotos ligger lokalt i /public/placeholders og optimeres af
  // next/image uden ekstern konfiguration. I produktion tilføjes Esmarks
  // billed-CDN her under images.remotePatterns.
};

export default nextConfig;
