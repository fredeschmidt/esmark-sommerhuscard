/** @type {import('next').NextConfig} */
const nextConfig = {
  // Placeholder photos live locally in /public/placeholders and are optimized by
  // next/image without external configuration. In production, Esmark's
  // image CDN is added here under images.remotePatterns.
};

export default nextConfig;
